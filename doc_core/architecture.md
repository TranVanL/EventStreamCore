# 🏗️ System Architecture

---

## 📐 High-Level Architecture

```

┌────────────────────────────────────────────────────────────────────────┐
│                         INGEST LAYER                                    │
│  ┌─────────────────────────────┐    ┌─────────────────────────────┐    │
│  │        TCP Server           │    │        UDP Server           │    │
│  │   • Port 9000               │    │   • Port 9001               │    │
│  │   • Multi-threaded          │    │   • recvmmsg batch          │    │
│  │   • Backpressure tracking   │    │   • Zero-copy               │    │
│  └──────────────┬──────────────┘    └──────────────┬──────────────┘    │
│                 │                                   │                   │
│                 └───────────────┬───────────────────┘                   │
│                                 ▼                                       │
│                    ┌─────────────────────────┐                         │
│                    │    IngestEventPool      │                         │
│                    │  (uses NUMAEventPool)   │                         │
│                    │  • NUMA-local memory    │                         │
│                    │  • O(1) acquire         │                         │
│                    └────────────┬────────────┘                         │
└─────────────────────────────────┼───────────────────────────────────────┘
                                  │
                                  ▼
┌────────────────────────────────────────────────────────────────────────┐
│                          CORE ENGINE                                    │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │                        Dispatcher                                  │ │
│  │   Route by EventPriority → REALTIME / TRANSACTIONAL / BATCH       │ │
│  └───────────────────────────────┬───────────────────────────────────┘ │
│                                  │                                      │
│  ┌───────────────────────────────▼──────────────────────────────────┐  │
│  │                      EventBusMulti                                │  │
│  │  ┌─────────────────┬─────────────────┬─────────────────┐         │  │
│  │  │    REALTIME     │  TRANSACTIONAL  │      BATCH      │         │  │
│  │  │   SPSC Queue    │   MPSC Queue    │   MPSC Queue    │         │  │
│  │  │  Cap: 16384     │   Cap: 65536    │   Cap: 65536    │         │  │
│  │  │  CRITICAL/HIGH  │     MEDIUM      │    LOW/BATCH    │         │  │
│  │  │    ~8ns         │     ~20ns       │     ~20ns       │         │  │
│  │  └────────┬────────┴────────┬────────┴────────┬────────┘         │  │
│  └───────────┼─────────────────┼─────────────────┼──────────────────┘  │
│              │                 │                 │                      │
│              ▼                 ▼                 ▼                      │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │                      ProcessManager                                │ │
│  │  ┌─────────────────┬─────────────────┬─────────────────┐          │ │
│  │  │RealtimeProcessor│TransactionalProc│ BatchProcessor  │          │ │
│  │  │ • AlertHandler  │ • LockFreeDedup │ • Window 5s     │          │ │
│  │  │ • SLA < 100µs   │ • Retry 3x      │ • Aggregation   │          │ │
│  │  │ • Thread pinned │ • max_retries=3 │ • Topic grouping│          │ │
│  │  └─────────────────┴─────────────────┴─────────────────┘          │ │
│  └───────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────┬───────────────────────────────────────┘
                                  │
                                  ▼
┌────────────────────────────────────────────────────────────────────────┐
│                          OUTPUT LAYER                                   │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐         │
│  │  StorageEngine  │  │  MetricRegistry │  │ DeadLetterQueue │         │
│  │ data/events.bin │  │  • Counter      │  │ • Failed events │         │
│  │ data/dlq_log.txt│  │  • Histogram    │  │ • Drop reason   │         │
│  │ • Binary format │  │  • Queue depth  │  │ • Timestamp     │         │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘         │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Event Flow Sequence

### Complete Event Lifecycle

```
┌────────┐     ┌────────┐     ┌────────┐     ┌────────┐     ┌────────┐
│ Client │     │ Ingest │     │ Pool   │     │  Bus   │     │Processor│
└───┬────┘     └───┬────┘     └───┬────┘     └───┬────┘     └───┬────┘
    │              │              │              │              │
    │  TCP/UDP     │              │              │              │
    │─────────────►│              │              │              │
    │              │              │              │              │
    │              │  acquire()   │              │              │
    │              │─────────────►│              │              │
    │              │  EventPtr    │              │              │
    │              │◄─────────────│              │              │
    │              │  (~15ns)     │              │              │
    │              │              │              │              │
    │              │  Parse frame & set priority │              │
    │              │──────────────────────────────────────────  │
    │              │              │              │              │
    │              │              │  push(QueueId, event)       │
    │              │              │─────────────►│              │
    │              │              │  (~8-20ns)   │              │
    │              │              │              │              │
    │              │              │              │  pop(timeout)│
    │              │              │              │─────────────►│
    │              │              │              │              │
    │              │              │              │              │ process()
    │              │              │              │              │ (~500ns)
    │              │              │              │              │
    │              │              │              │  store()     │
    │              │              │              │◄─────────────│
    │◄─────────────┼──────────────┼──────────────┼──────────────│
    │              │              │              │     Done     │
```

### Timing Breakdown (P99)

```
Event Lifecycle Timeline
═══════════════════════════════════════════════════════════════════════►
│                                                                    Time
├─[0ns]─────── Receive TCP/UDP ──────────────────────────────────────┤
├─[15ns]────── Pool Acquire (NUMAEventPool) ─────────────────────────┤
├─[50ns]────── Frame Parse + Priority Extract ───────────────────────┤
├─[70ns]────── Queue Push (SPSC ~8ns / MPSC ~20ns) ──────────────────┤
├─[150ns]───── Worker Pop ───────────────────────────────────────────┤
├─[200ns]───── Dispatch to Processor ────────────────────────────────┤
├─[500ns]───── Processing (varies by type) ──────────────────────────┤
├─[1.5µs]───── Storage Write (if enabled) ───────────────────────────┤
├─[2µs]─────── Complete ─────────────────────────────────────────────┤
═══════════════════════════════════════════════════════════════════════►
                           Total P99 < 2µs
```

---

## 🎯 Component Details

### 1. EventBusMulti (3 Queues)

```cpp
// File: include/eventstream/core/events/event_bus.hpp
class EventBusMulti {
public:
    enum class QueueId : int { 
        REALTIME = 0,       // SPSC - CRITICAL/HIGH priority
        TRANSACTIONAL = 1,  // MPSC - MEDIUM priority
        BATCH = 2           // MPSC - LOW/BATCH priority
    };

    enum class OverflowPolicy : int { 
        DROP_OLD = 0,       // Drop oldest (default)
        BLOCK_PRODUCER = 1, // Block until space
        DROP_NEW = 2        // Drop incoming
    };

    enum class PressureLevel : int { 
        NORMAL = 0, 
        HIGH = 1, 
        CRITICAL = 2 
    };
    
private:
    // REALTIME: SpscRingBuffer<EventPtr, 16384> - Wait-free
    // TRANSACTIONAL: MPSC with mutex + condition_variable
    // BATCH: MPSC with mutex + condition_variable
    RealtimeQueue RealtimeBus_;
    Q TransactionalBus_;
    Q BatchBus_;
    DeadLetterQueue dlq_;
    int numa_node_ = -1;
};
```

### 2. ProcessManager (Wire Dependencies)

```cpp
// File: include/eventstream/core/processor/process_manager.hpp
class ProcessManager {
public:
    struct Dependencies {
        StorageEngine* storage = nullptr;           // Durable persistence
        EventStream::DeadLetterQueue* dlq = nullptr; // Failed events
        EventStream::AlertHandlerPtr alert_handler = nullptr;
        std::chrono::seconds batch_window{5};       // Batch window size
    };
    
    explicit ProcessManager(EventStream::EventBusMulti& bus);
    ProcessManager(EventStream::EventBusMulti& bus, const Dependencies& deps);
    
    void start();  // Spawn 3 processor threads
    void stop();   // Stop all processors
    
private:
    std::unique_ptr<RealtimeProcessor> realtimeProcessor_;
    std::unique_ptr<TransactionalProcessor> transactionalProcessor_;
    std::unique_ptr<BatchProcessor> batchProcessor_;
    
    std::thread realtimeThread_;     // Pinned to CPU 0
    std::thread transactionalThread_; // Pinned to CPU 1
    std::thread batchThread_;        // Pinned to CPU 2
};
```

### 3. ControlPlane (5-Level Backpressure)

```cpp
// File: include/eventstream/core/control/control_plane.hpp
class ControlPlane {
public:
    EventControlDecision evaluateMetrics(
        uint64_t queue_depth,      // Current queue size
        uint64_t total_processed,  // Events processed
        uint64_t total_dropped,    // Events dropped
        uint64_t latency_ms        // P99 latency
    );
    
    void executeDecision(
        const EventControlDecision& decision,
        PipelineStateManager& state_manager
    );
    
private:
    ControlThresholds thresholds_;
    FailureState previous_state_ = FailureState::HEALTHY;  // Hysteresis
};
```

**5-Level State Machine:**

```
                    Recovery (hysteresis 80%)
        ┌──────────────────────────────────────────────────────────┐
        │                                                          │
        ▼                                                          │
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   HEALTHY   │───►│  ELEVATED   │───►│  DEGRADED   │───►│  CRITICAL   │───►│  EMERGENCY  │
│ queue < 50% │    │ queue < 75% │    │ queue < 100%│    │ queue = 100%│    │ queue > 150%│
│ drop < 1%   │    │ drop < 2%   │    │ drop < 4%   │    │ drop >= 4%  │    │ drop >= 10% │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
     │                   │                   │                   │                   │
     │                   │                   │                   │                   │
     ▼                   ▼                   ▼                   ▼                   ▼
  Normal          Increase workers     Pause trans        Drop batch       Emergency drop
```

### 4. StorageEngine (Persistence)

```cpp
// File: include/eventstream/core/storage/storage_engine.hpp
class StorageEngine {
public:
    StorageEngine(const std::string& storagePath, const std::string& dlqPath = "");
    
    void storeEvent(const Event& event);      // Binary: data/events.bin
    void appendDLQ(const std::vector<EventPtr>& events, const std::string& reason);
    void flush();  // Batch flush (every 100 events)
    
private:
    std::ofstream storageFile;   // Binary event storage
    std::ofstream dlqFile;       // DLQ text log (derived from storagePath)
    static constexpr size_t FLUSH_BATCH_SIZE = 100;
};
```

---

## 📊 Memory Layout

### Cache Line Optimization

```
┌─────────────────────────────────────────────────────────────────────┐
│                     SPSC RING BUFFER LAYOUT                          │
│                                                                      │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │ Cache Line 0 (64 bytes) - PRODUCER                             │ │
│  │ ┌──────────────────────────────────────────────────────────┐   │ │
│  │ │  alignas(64) atomic<size_t> head_{0}  │  padding         │   │ │
│  │ │  [Only producer writes]               │  (56 bytes)      │   │ │
│  │ └──────────────────────────────────────────────────────────┘   │ │
│  ├────────────────────────────────────────────────────────────────┤ │
│  │ Cache Line 1 (64 bytes) - CONSUMER                             │ │
│  │ ┌──────────────────────────────────────────────────────────┐   │ │
│  │ │  alignas(64) atomic<size_t> tail_{0}  │  padding         │   │ │
│  │ │  [Only consumer writes]               │  (56 bytes)      │   │ │
│  │ └──────────────────────────────────────────────────────────┘   │ │
│  ├────────────────────────────────────────────────────────────────┤ │
│  │ Cache Lines 2+ (128KB for 16K events)                          │ │
│  │ ┌──────────────────────────────────────────────────────────┐   │ │
│  │ │  alignas(64) T buffer_[Capacity]                         │   │ │
│  │ │  [Sequential access - cache prefetch friendly]           │   │ │
│  │ └──────────────────────────────────────────────────────────┘   │ │
│  └────────────────────────────────────────────────────────────────┘ │
│                                                                      │
│  ❌ FALSE SHARING                    ✅ CACHE ALIGNED                │
│  ┌────────────────────┐              ┌────────────────────┐         │
│  │ head_ │ tail_      │              │ head_    │ padding │         │
│  │ [Same cache line]  │              ├──────────┼─────────┤         │
│  │ [Both CPUs fight]  │              │ tail_    │ padding │         │
│  │ [Invalidation!!]   │              │ [Separate lines!]  │         │
│  └────────────────────┘              └────────────────────┘         │
└─────────────────────────────────────────────────────────────────────┘
```

### MPSC Queue Node Layout

```
┌─────────────────────────────────────────────────────────────────────┐
│                      MPSC VYUKOV ALGORITHM                           │
│                                                                      │
│  Initial state (empty with dummy):                                   │
│                                                                      │
│   head_────►┌─────────────┐◄────tail_                               │
│             │  DUMMY NODE │                                          │
│             │  data: -    │                                          │
│             │  next: null │                                          │
│             └─────────────┘                                          │
│                                                                      │
│  After push(A):                                                      │
│                                                                      │
│   head_────►┌─────────────┐    ┌─────────────┐◄────tail_            │
│             │  DUMMY NODE │───►│   NODE A    │                       │
│             │  next: A    │    │  data: A    │                       │
│             └─────────────┘    │  next: null │                       │
│                                └─────────────┘                       │
│                                                                      │
│  Push is lock-free (exchange on tail_):                              │
│    prev = tail_.exchange(new_node)  // Atomic                        │
│    prev->next.store(new_node)       // Link                          │
│                                                                      │
│  Pop moves head_ to next node (single consumer):                     │
│    next = head_->next.load()        // Read next                     │
│    item = next->data                // Get data                      │
│    head_.store(next)                // Advance head                  │
│    delete old_head                  // Free dummy                    │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔧 Configuration (ControlThresholds)

```cpp
// File: include/eventstream/core/control/thresholds.hpp
struct ControlThresholds {
    uint64_t max_queue_depth = 5000;           // Trigger threshold
    double max_drop_rate = 2.0;                // Max acceptable %
    uint64_t max_latency_ms = 100;             // For future use
    uint64_t min_events_for_evaluation = 1000; // Warmup period
    double recovery_factor = 0.8;              // Hysteresis (80%)
};
```

**Threshold Transitions:**

| From State | To State | Condition |
|------------|----------|-----------|
| HEALTHY | ELEVATED | drop_rate > 1% OR queue > 50% |
| ELEVATED | DEGRADED | drop_rate > 2% OR queue > 75% |
| DEGRADED | CRITICAL | drop_rate ≥ 4% OR queue ≥ 100% |
| CRITICAL | EMERGENCY | drop_rate ≥ 10% OR queue > 150% |
| Any | Previous | metrics < threshold * 0.8 (hysteresis) |

---

## 📈 Metrics (MetricRegistry)

| Metric Name | Type | Description |
|-------------|------|-------------|
| `events_received_total` | Counter | Total events ingested |
| `events_processed_total` | Counter | Total successfully processed |
| `events_dropped_total` | Counter | Events dropped (backpressure) |
| `queue_depth_{realtime,trans,batch}` | Gauge | Current queue sizes |
| `latency_ns_{realtime,trans,batch}` | Histogram | Processing latency |
| `dedup_hits` | Counter | Duplicate events filtered |
| `backpressure_drops` | Counter | TCP/UDP backpressure drops |

---

## ➡️ Next

- [Event Model & Protocol →](event.md)
